# 📡 Today Signal (알고리즘 매매 신호) 개발자 가이드

안녕하세요! 이 프로젝트는 매일 아침과 장 중에 **"오늘 이 종목이 오를 것 같다"**는 신호를 알고리즘으로 계산해주는 프로젝트입니다. 여기서 나온 신호가 `KIS-Trader`로 넘어가서 자동 매매가 이루어집니다.

---

## 🏗 신호 생성 및 전달 과정 (The Pipeline)

이 서비스는 단순한 정보성 앱이 아니라, 자동 매매 시스템의 **'두뇌'** 역할을 합니다.

1.  **데이터 분석**: `scripts/collect-data.ts`가 KIS API를 사용해 당일 수급, 거래량 상위 종목 등을 스코어링합니다.
2.  **데이터 확정**: 최종 선정된 종목들은 `public/data/today_signal.json`에 저장됩니다.
3.  **데이터 셔틀 (중요!)**: `scheduler` 프로젝트에 의해 이 JSON 파일이 **`c:/github/kis-trader/data/`** 폴더로 자동 복사됩니다. (이걸 통해 실전 매매 로직이 가동됩니다.)

---

## 🛠 주요 명령어 및 실행 가이드

### 1. 신호 갱신
- `npm run data:update`: 지금 즉시 시장 상황을 분석해서 새로운 추천 종목을 뽑아내고 싶을 때 실행합니다.
- `npm run short:balance`: 공매도 잔고 등 위험 지표를 따로 수집해서 데이터에 반영합니다.

### 2. UI 확인 및 빌드
- `npm run dev`: 오늘 어떤 종목들이 뽑혔는지 예쁜 웹 화면으로 미리 확인하고 싶을 때 사용합니다.

---

## 💡 개발 시 유의사항 및 알고리즘 팁

### 어떤 종목이 추천되나요? (Logic)
- **수급 강화**: 외국인과 기관이 최근 며칠간 꾸준히 사모으고 있는 종목.
- **모멘텀 폭발**: 당일 아침에 거래량이 전일 전체 거래량을 이미 넘어선 종목.
- 이 알고리즘을 고치고 싶다면 `scripts/collect-data.ts` 내부의 점수 계산 로직을 확인하세요.

### 트레이더와 발을 맞춰야 해요 (Consistency)
- 여기서 내보내는 JSON 형식(필드명 등)이 바뀌면 `KIS-Trader`가 에러를 냅니다. 데이터를 수정할 땐 두 프로젝트를 필히 같이 점검해야 합니다.

---

## 🚨 문제 해결 (Troubleshooting)

**Q: 추천된 종목이 하나도 없어요.**
- A: 코스피/코스닥 지수가 너무 안 좋아서 '하락장 필터'가 작동했거나, 분석 데이터(API 응답)가 유효하지 않을 수 있습니다. 로그를 통해 수집 단계의 에러를 확인하세요.

**Q: 셔틀이 작동하지 않아서 트레이더가 옛날 종목으로 매매해요.**
- A: 스케줄러(`c:/github/scheduler`)에서 파일 복사 로직(`run-today-signal.mjs`)에 권한 문제가 없는지, 경로가 올바른지 확인해야 합니다.

---

## 📁 주요 폴더 설명
- `scripts/`: 이 프로젝트의 핵심인 신호 추출 알고리즘 파일들이 모여 있습니다.
- `public/data/`: `today_signal.json`이 담기는 곳으로, 모든 매매 신호의 정수가 여기 담깁니다.
